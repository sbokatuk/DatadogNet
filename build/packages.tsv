# The package set, and the single source of truth for it.
#
# BuildNugets.sh packs in this order, the release and pull-request workflows generate their package
# tables from it, the package tests read it, and the device-test runner scripts clear the local
# NuGet cache for every id listed here. Adding a package means adding a row here and a project under
# src/ - nothing else keeps a second copy of the list.
#
# Rows are in dependency order: a package's deps must already have appeared above it.
#
# Columns
#   id      the NuGet package id, which is also the directory under src/ and the .csproj name
#   heads   which target-framework families the package ships, across net8, net9 and net10:
#             all     - the platform-neutral asset of each, plus its android, ios and maccatalyst
#                       heads (twelve)
#             maui    - android, ios, maccatalyst and windows heads (twelve), no neutral asset
#   deps    other ids in this repository it depends on, comma separated, or -
#
# DatadogNet.Maui is 'maui' rather than 'all' because Microsoft.Maui.Controls is a workload
# metapackage with no platform-neutral assets - there is nothing to compile a no-op MAUI shim
# against at any version. Its Windows head IS shipped, as a stub over the core package's no-op
# neutral implementation, compiled from macOS via EnableWindowsTargeting - which is what lets a
# multi-headed app reference the package with no platform condition. See its .csproj.
#
# id	heads	deps
DatadogNet	all	-
DatadogNet.Extensions.DependencyInjection	all	DatadogNet
DatadogNet.Extensions.Diagnostics	all	DatadogNet
DatadogNet.CrashReporting	all	DatadogNet
DatadogNet.WebView	all	DatadogNet
DatadogNet.Maui	maui	DatadogNet,DatadogNet.Extensions.DependencyInjection
