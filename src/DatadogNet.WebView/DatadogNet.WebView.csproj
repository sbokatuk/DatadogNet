<Project Sdk="Microsoft.NET.Sdk">

  <Import Project="../Datadog.Facade.props" />

  <PropertyGroup>
    <PackageId>DatadogNet.WebView</PackageId>
    <Title>Datadog web view tracking for .NET MAUI</Title>
    <Description>
      Bridges RUM events and logs out of an Android WebView or a WKWebView into the surrounding
      native Datadog session. For hybrid apps; separate from DatadogNet so an app without a web view
      does not carry it.
    </Description>
    <PackageTags>datadog;maui;webview;rum;hybrid;android;ios</PackageTags>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../DatadogNet/DatadogNet.csproj" />
  </ItemGroup>

  <ItemGroup Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'android'">
    <PackageReference Include="DatadogNet.WebView.Android" Version="$(DatadogNetAndroidVersion)" />
  </ItemGroup>

  <ItemGroup Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'ios'">
    <PackageReference Include="DatadogNet.WebViewTracking.iOS" Version="$(DatadogNetiOSVersion)" />
  </ItemGroup>

  <!-- Mac Catalyst has WKWebView, so web view tracking works exactly as on iOS. -->
  <ItemGroup Condition="$([MSBuild]::GetTargetPlatformIdentifier('$(TargetFramework)')) == 'maccatalyst'">
    <PackageReference Include="DatadogNet.WebViewTracking.Mac" Version="$(DatadogNetMacVersion)" />
  </ItemGroup>

  <!--
    Consumer R8/ProGuard keep-rules for the Android head. Packed under buildTransitive/ so NuGet
    imports the .targets into every consuming project; the rules attach only where R8 runs. See
    the files' own comments for why they exist and why they are duplicated from the binding.
  -->
  <ItemGroup>
    <None Include="buildTransitive/DatadogNet.WebView.targets" Pack="true" PackagePath="buildTransitive" />
    <None Include="buildTransitive/datadog-webview.pro" Pack="true" PackagePath="buildTransitive" />
  </ItemGroup>

</Project>
